public String getAccountBillingCityWithSafeNavigation(List<Account> accounts){
 String billingCity ;
 for (account ac : accounts){
 if(billingCity == NULL AND ac.BillingCity == NULL){
 billingCity = ac.BillingCity;
 billingCity = billingCity.toUpperCase();}
}
 return (billingCity);
}
